hp = 25;             // total health
shoot_delay = 30;    // frames between shots
move_speed = 4;      // fast — faster than the player's max speed
shoot_delay = 45;
image_angle = 0;

// --- Entry state ---
entering = true;
target_x = room_width / 2;
target_y = room_height / 2;
entry_speed = 6;     // FAST entry — its a quick ship

